# SMTP Configuration (Set environment variables: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM)
mail.smtp.auth=true
mail.smtp.starttls.enable=true

# Database Configuration
db.url=jdbc:mysql://127.0.0.1:3306/lifeflow
db.user=root
db.password=root

# Google OAuth Configuration (Set environment variables: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI)
google.redirect.uri=http://localhost:8080/auth/google/callback
